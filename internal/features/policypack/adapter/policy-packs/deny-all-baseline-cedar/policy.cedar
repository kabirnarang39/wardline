// Denies every call: no permit statement exists, and Cedar's own
// semantics are default-deny with no matching permit -- no adapter-side
// work required. Add permit statements below as you grant access.
