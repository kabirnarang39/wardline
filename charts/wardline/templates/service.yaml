apiVersion: v1
kind: Service
metadata:
  name: {{ include "wardline.fullname" . }}
  labels:
    {{- include "wardline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wardline.selectorLabels" . | nindent 4 }}
