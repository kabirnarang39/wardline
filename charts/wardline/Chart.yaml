apiVersion: v2
name: wardline
description: Wardline — an AI-agent control-plane proxy (policy, budget, audit, tracing) in front of MCP servers
type: application
version: 0.1.0
appVersion: "0.5.0-dev"
